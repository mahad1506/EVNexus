# Backend

This folder is reserved for the EVNexus backend API and server-side logic.

The current project does not contain a backend implementation yet. The frontend assistant currently calls `/api/chat`, which should be implemented here when the AI service is connected.